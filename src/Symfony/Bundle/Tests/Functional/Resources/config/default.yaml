framework:
    router:
        utf8: true

async_aws:
    config:
        region: eu-central-1
    services:
        sqs:
            config:
                region: us-west-1
        ses: ~
        foobar:
            type: sqs
