framework:
    session:
        enabled: false
    router:
        utf8: true
async_aws:
    config:
        region: eu-central-1
        accessKeyId: key
        accessKeySecret: secret
    clients:
        ses: ~
        s3:
            config:
                region: us-west-1

        sqs:
            config:
                accessKeyId: ~
                accessKeySecret: ~
